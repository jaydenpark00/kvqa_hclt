# Install a CUDA-matched PyTorch first, e.g. for CUDA 12.4:
#   pip install torch==2.6.0 torchvision --index-url https://download.pytorch.org/whl/cu124
# Tested configuration:
torch==2.6.0
torchvision
transformers==5.16.1
accelerate>=1.0
numpy>=1.26
pillow>=10.0
pyyaml>=6.0
setproctitle>=1.3
# MiniLM is loaded via transformers (mean pooling) — sentence-transformers is NOT required.
